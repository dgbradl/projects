Generate {batchSize} evocative fantasy names for {kind}s. Each is 1-4 words.
Lean into specificity, half-remembered local color, place-based naming.
Coin entirely new names. Do not reuse an existing name, and do not derive a
name by adding to or swapping a word on one that already exists.
{avoidBlock}
Style hint: {styleHint}

JSON shape: {"names": ["...", "...", ...]}
